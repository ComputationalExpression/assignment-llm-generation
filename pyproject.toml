[project]
name = "assignment-llm-template"
version = "1.0.0"
requires-python= ">=3.10"
dependencies = [
    "black>=26.3.1",
    "gatorgrade",
    "mypy>=1.19.1",
    "pytest",
]

[tool.pytest]
pythonpath = [
    "src"
]
testpaths = [
    "tests"
]
